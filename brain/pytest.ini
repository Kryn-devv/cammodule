[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
# The camera package is added to sys.path by tests/conftest.py, which also
# drives the async tests so this suite needs no pytest plugins at all.
